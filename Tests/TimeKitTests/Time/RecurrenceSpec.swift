

import TimeKit
import Quick
import Nimble

class RecurrenceSpec: QuickSpec {
    
    override func spec() {
        
        describe("Recurrence") {
            
            it("supports translation to String") {
                
                //expect(Time.monday.subtracting(days: 2))
                //.to(equal(WeekDay.saturday))
            }
            
            it("can be interpreted from String") {
                
                //expect(Time.monday.subtracting(days: 2))
                //.to(equal(WeekDay.saturday))
            }
            
            it("can be transformed to other models") {
                
                //expect(Time.monday.subtracting(days: 2))
                //.to(equal(WeekDay.saturday))
            }
            
            it("can be transformed to and init with exact time interval") {
                
                //expect(Time.monday.subtracting(days: 2))
                //.to(equal(WeekDay.saturday))
            }
        }
    }
}
