

import TimeKit
import Quick
import Nimble

class AnyPeriodSpec: QuickSpec {
    
    override func spec() {
        
        describe("AnyPeriod") {
            it("supports translation to String") {
                
                //expect(Time.monday.subtracting(days: 2))
                //.to(equal(WeekDay.saturday))
            }
            
            it("can be interpreted from String") {
                
                //expect(Time.monday.subtracting(days: 2))
                //.to(equal(WeekDay.saturday))
            }
            
            it("can be transformed to other models") {
                
                //expect(Time.monday.subtracting(days: 2))
                //.to(equal(WeekDay.saturday))
            }
            
            it("can be transformed to and init with exact time interval or period") {
                
                //expect(Time.monday.subtracting(days: 2))
                //.to(equal(WeekDay.saturday))
            }
        }
    }
}
