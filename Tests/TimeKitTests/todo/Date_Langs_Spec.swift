

import TimeKit
import Quick
import Nimble


class Date_Langs_Spec: QuickSpec {
    
    override func spec() {
        
        describe("Date") {
            
            it("testLanguages") {
                //expect(result).to(equal(...))
                //
                //        RelFormatter.allLanguages.forEach { lang in
                //
                //            expect((lang.id.hasSth), "Language \(lang.id) has not a valid id")
                //
                //            lang.translations.forEach{ (key, value) in
                //
                //                if RelFormatter.TranslStyle(rawValue: key) == nil {
                //                    fail("TranslSet Style '\(key)' is not supported by the library (lang '\(lang.id)')")
                //                    return
                //                }
                //                guard let flavourDict = value as? [String: Any] else {
                //                    fail("TranslSet Style dictionary '\(key)' is not a valid dictionary")
                //                    return
                //                }
                //                flavourDict.keys.forEach{ rawTimeUnit in
                //
                //                    if RelFormatter.Unit(rawValue: rawTimeUnit) == nil {
                //                        fail("Time unit '\(rawTimeUnit)' in lang \(key) is not a valid")
                //                        return
                //                    }
                //                }
                //            }
                //        }
            }
            
            it("testValues") {
                //expect(result).to(equal(...))
                //        let ago5Mins = Date() - 5.minutes
                //        let value1 = ago5Mins.relString(RelFormatter.defaultStyle, LID.italian) // "5 minuti fa"
                //        expect(value1 == "5 minuti fa", "Failed to get relative date in default mode")
                //
                //        let justNow2 = Date() - 2.hours
                //        let value2 = justNow2.relString(RelFormatter.twitterStyle, LID.italian) // "2h fa"
                //        expect(value2 == "2h fa", "Failed to get relative date in twitter mode")
                //
                //        let justNow = Date() - 10.seconds
                //        let value3 = justNow.relString(RelFormatter.twitterStyle, LID.italian) // "ora"
                //        expect(value3 == "ora", "Failed to get relative date in twitter mode")
                //
                //        let value4 = justNow.relString(RelFormatter.twitterStyle, LID.english) // "now"
                //        expect(value4 == "now", "Failed to get relative date in twitter mode")
                //
            }
        }
    }
}
